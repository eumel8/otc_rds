---
- hosts: localhost
  gather_facts: no
  ignore_errors: true

  roles:
    - role: "otc_auth"
    - role: "otc_rds"
      rds_name: "ansible-mysql01"
      localaction: "delete"
      uri_retries: 1
